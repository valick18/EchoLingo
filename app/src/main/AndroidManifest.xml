<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.germanspreach">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_main_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_main_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".FlashcardActivity" android:exported="false"/>
        <activity android:name=".LearnedActivity" android:exported="false"/>
        <activity android:name=".StudyActivity" android:exported="false"/>
        <activity android:name=".ListenModeActivity" android:exported="false"/>
        <activity android:name=".SettingsActivity" android:exported="false"/>

    </application>

</manifest>